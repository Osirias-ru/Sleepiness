const { EmbedBuilder } = require('discord.js');

module.exports = {
    name: "", // Название команды(что ввести в чат для выполнения)
    type: 3, // Вид команды(оставлять)
    run: async (client, interaction, config, db) => {
        // что делать

    },
};
